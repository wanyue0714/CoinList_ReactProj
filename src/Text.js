import {fontColorWhite, fontSize1, textAlignCenter} from "./style";
import styled from 'styled-components';

export const WhiteText = styled.h4`
     ${textAlignCenter}
     ${fontSize1}
     ${fontColorWhite}
`;
